services:
  # Backend API Service (Docker-based for reliable geospatial support)
  - type: web
    name: delhi-pollution-api
    runtime: docker
    plan: free
    region: oregon
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: PORT
        value: 10000
